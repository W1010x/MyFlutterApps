class Question {
  String questionText;
  bool questionAnswer;
  // build constructor
  Question(String q, bool a ){ //no need for {} so we delete it and create new file that hold answer and Q its called quizBrain.dart
  questionText= q;
  questionAnswer= a;
}
}

